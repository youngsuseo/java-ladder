package ladder.domain;

import java.util.List;
import java.util.Map;

public class LadderResult {

//    public static Players resultPrize(Players players, Prizes prizes, Ladder ladder) {
//        List<String> prizeList = prizes.getPrizes(); // FIXME linked list
//
//        String temp;
//        for (int i = ladder.getVerticalLines().size() - 1; i >= 0; i--) {
//            HorizontalLines horizontalLines = ladder.getVerticalLines().get(i);
//            for (int j = 0; j < horizontalLines.getHorizontalLines().size(); j++) {
//                if (horizontalLines.getHorizontalLines().get(j)) {
//                    temp = prizeList.get(j);
//                    prizeList.set(j, prizeList.get(j + 1));
//                    prizeList.set(j + 1, temp);
//                }
//            }
//        }
//
//        Map<PlayerName, String> playerMap = players.getPlayers();
//
//        int index = 0;
//        for (PlayerName playerName : playerMap.keySet()) {
//            playerMap.put(playerName, prizeList.get(index++));
//        }
//
//        return players;
//    }
}
